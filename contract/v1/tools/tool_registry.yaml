version: v1
contract_version: v1
last_updated: 2026-02-23

tools:

  # --------------------------
  # INTERVIEW CAPTURE
  # --------------------------

  - tool_name: dictation.start
    status: placeholder
    implementation_status: not_implemented
    description: "Begin audio capture for a client interview session."
    idempotent: false
    timeout_default_ms: 30000
    allowed_lanes: [TOOL_INTERVIEW_CAPTURE]
    required_scope_keys: [case_id, client_session_id, run_id]
    prohibited_flags: [cross_case_lookup, external_export]
    write_targets: []

  - tool_name: dictation.stop
    status: placeholder
    implementation_status: not_implemented
    description: "Stop audio capture for a client interview session."
    idempotent: true
    timeout_default_ms: 30000
    allowed_lanes: [TOOL_INTERVIEW_CAPTURE]
    required_scope_keys: [case_id, client_session_id, run_id]
    prohibited_flags: [cross_case_lookup, external_export]
    write_targets: []

  - tool_name: transcription.run
    status: placeholder
    implementation_status: not_implemented
    description: "Submit captured audio for transcription and return a job handle."
    idempotent: true
    timeout_default_ms: 60000
    allowed_lanes: [TOOL_INTERVIEW_CAPTURE]
    required_scope_keys: [case_id, client_session_id, run_id]
    prohibited_flags: [cross_case_lookup, external_export]
    write_targets: []
    returns_job_handle: true

  - tool_name: transcription.status
    status: placeholder
    implementation_status: not_implemented
    description: "Poll transcription job status and retrieve results when ready."
    idempotent: true
    timeout_default_ms: 30000
    allowed_lanes: [TOOL_INTERVIEW_CAPTURE]
    required_scope_keys: [case_id, client_session_id, run_id]
    prohibited_flags: [cross_case_lookup, external_export]
    write_targets: []
    consumes_job_handle: true

  - tool_name: interview.prompt.next
    status: placeholder
    implementation_status: not_implemented
    description: "Return next governed prompt for structured interviewing."
    idempotent: true
    timeout_default_ms: 10000
    allowed_lanes: [TOOL_INTERVIEW_CAPTURE]
    required_scope_keys: [case_id, client_session_id, run_id]
    prohibited_flags: [cross_case_lookup, external_export]
    write_targets: []

  # --------------------------
  # EXPORT BUNDLE
  # --------------------------

  - tool_name: export_bundle.create
    status: placeholder
    implementation_status: not_implemented
    description: "Create an internal export bundle from approved case artifacts for attorney review and downstream governed use."
    idempotent: true
    timeout_default_ms: 60000
    allowed_lanes: [TOOL_EXPORT_BUNDLE]
    required_scope_keys: [case_id, run_id]
    prohibited_flags: [cross_case_lookup, external_export]
    write_targets: []

  - tool_name: export_bundle.download
    status: placeholder
    implementation_status: not_implemented
    description: "Download an internal export bundle that has already been created and approved for governed use."
    idempotent: true
    timeout_default_ms: 60000
    allowed_lanes: [TOOL_EXPORT_BUNDLE]
    required_scope_keys: [case_id, run_id]
    prohibited_flags: [cross_case_lookup, external_export]
    write_targets: []

  - tool_name: export_bundle.signoff
    status: placeholder
    implementation_status: not_implemented
    description: "Record attorney signoff or governed approval state for an internal export bundle."
    idempotent: true
    timeout_default_ms: 30000
    allowed_lanes: [TOOL_EXPORT_BUNDLE]
    required_scope_keys: [case_id, run_id]
    prohibited_flags: [cross_case_lookup, external_export]
    write_targets: []

  - tool_name: export_bundle.validate
    status: placeholder
    implementation_status: not_implemented
    description: "Validate that an internal export bundle is complete, policy-compliant, and ready for governed use."
    idempotent: true
    timeout_default_ms: 30000
    allowed_lanes: [TOOL_EXPORT_BUNDLE]
    required_scope_keys: [case_id, run_id]
    prohibited_flags: [cross_case_lookup, external_export]
    write_targets: []

  # --------------------------
  # OVERRIDE CONTROL
  # --------------------------

  - tool_name: override.enable
    status: placeholder
    implementation_status: not_implemented
    description: "Enable a governed override within the allowed case and run scope."
    idempotent: true
    timeout_default_ms: 30000
    allowed_lanes: [TOOL_OVERRIDE_CONTROL]
    required_scope_keys: [case_id, run_id]
    prohibited_flags: [cross_case_lookup, external_export]
    write_targets: []

  - tool_name: override.disable
    status: placeholder
    implementation_status: not_implemented
    description: "Disable a previously applied governed override within the allowed case and run scope."
    idempotent: true
    timeout_default_ms: 30000
    allowed_lanes: [TOOL_OVERRIDE_CONTROL]
    required_scope_keys: [case_id, run_id]
    prohibited_flags: [cross_case_lookup, external_export]
    write_targets: []
    
  # --------------------------
  # RERUN CONTROL
  # --------------------------

  - tool_name: rerun.force
    status: placeholder
    implementation_status: not_implemented
    description: "Force a governed rerun of a permitted workflow or artifact generation step within the allowed case and run scope."
    idempotent: false
    timeout_default_ms: 30000
    allowed_lanes: [TOOL_RERUN_CONTROL]
    required_scope_keys: [case_id, run_id]
    prohibited_flags: [cross_case_lookup, external_export]
    write_targets: []
    
  # --------------------------
  # RUN OVERRIDE
  # --------------------------
  - tool_name: run.state.override
    status: placeholder
    implementation_status: not_implemented
    description: "Apply a governed override to run state within the allowed case and run scope."
    idempotent: false
    timeout_default_ms: 30000
    allowed_lanes: [TOOL_RUN_STATE_CONTROL]
    required_scope_keys: [case_id, run_id]
    prohibited_flags: [cross_case_lookup, external_export]
    write_targets: []
    
  # --------------------------
  # TAXONOMY CONTROL
  # --------------------------

  - tool_name: taxonomy.unlock
    status: placeholder
    implementation_status: not_implemented
    description: "Unlock a governed taxonomy for authorized maintenance within the allowed case and run scope."
    idempotent: false
    timeout_default_ms: 30000
    allowed_lanes: [TOOL_TAXONOMY_CONTROL]
    required_scope_keys: [case_id, run_id]
    prohibited_flags: [cross_case_lookup, external_export]
    write_targets: []
  - tool_id: authority.sync.leginfo.ca_bpc_div10_cannabis
    display_name: "Authority Sync - CA BPC Division 10 Cannabis"
    description: "Refresh authoritative California BPC Division 10 Cannabis materials from LegInfo."
    category: authority_sync
    execution_mode: program
    entrypoint: "programs/authority_sync/program_CA_BPC_DIV10_CANNABIS_SYNC.py"
    writes_to:
      - "authority_store/ca/bpc/division_10_cannabis/current"
      - "authority_store/ca/bpc/division_10_cannABIS/raw"
      - "authority_store/ca/bpc/division_10_cannabis/runs"
      - "authority_store/ca/bpc/division_10_cannabis/state"
